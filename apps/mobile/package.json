{
  "name": "@yappie/mobile",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "echo 'mobile build - pending Expo setup (YAP-51)'",
    "dev": "echo 'mobile dev - pending Expo setup (YAP-51)'",
    "lint": "eslint . --no-error-on-unmatched-pattern",
    "test": "echo 'no tests configured yet'",
    "test:coverage": "echo 'no coverage configured yet'",
    "type-check": "echo 'no type-check configured yet'",
    "clean": "rm -rf .expo dist"
  },
  "dependencies": {
    "@yappie/shared": "workspace:*"
  }
}
